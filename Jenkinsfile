pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                //run this command abc
                echo 'Hello, World! Running main branch..!!'
            }
        }
    }
}
